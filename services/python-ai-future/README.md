# Future Python AI Services

These folders reserve the local and server-side integration points for:

- WhisperLive live transcription
- whisper_streaming adaptive live transcription
- faster-whisper local transcription
- WhisperX final pass with word timestamps
- pyannote.audio diarization

The MVP uses mock browser providers first so the product workflow can be validated before GPU services are deployed.
